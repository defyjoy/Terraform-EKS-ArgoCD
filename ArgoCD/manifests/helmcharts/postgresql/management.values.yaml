primary:
  pgHbaConfiguration: |
    host all all all trust
  persistence:
    storageClass: aws-ebs


global:
  defaultStorageClass: aws-ebs
  storageClass: aws-ebs

  postgresql:
    auth:
      postgresPassword: "postgresql@123"
      username: "zitadel_queries"
      password: "password@123"
      database: "zitadel"
      # existingSecret: ""
      # secretKeys:
      #   adminPasswordKey: ""
      #   userPasswordKey: ""
      #   replicationPasswordKey: ""